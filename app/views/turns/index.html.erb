<p id="notice"><%= notice %></p>

<h1>Turns</h1>

<table>
  <thead>
    <tr>
      <th>Production date</th>
      <th>Turn number</th>
      <th>Product</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @turns.each do |turn| %>
      <tr>
        <td><%= turn.production_date %></td>
        <td><%= turn.turn_number %></td>
        <td><%= turn.product %></td>
        <td><%= link_to 'Show', turn %></td>
        <td><%= link_to 'Edit', edit_turn_path(turn) %></td>
        <td><%= link_to 'Destroy', turn, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Turn', new_turn_path %>
